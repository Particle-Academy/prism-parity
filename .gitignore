node_modules/
dist/
vendor/
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/
build/
.coverage
composer.lock
package-lock.json

# Composer mirrors the path package into vendor/; `composer update` has been
# observed reporting success while silently keeping the old bytes, so the mirror
# is never a source of truth. Delete it and reinstall.
runners/php/vendor/
